- name: nginx install and start
  hosts: frontend
  become: yes
  tasks:
  - name: Install nginx server
    ansible.builtin.package:
      name: nginx
      state: present

  - name: start nginx
    ansible.builtin.service:
      name: ngnix
      state: started
      enabled: yes